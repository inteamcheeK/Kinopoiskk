@model Genre

@{
    ViewData["Title"] = "Изменить название жанра";
}

<div class="row">
    <div class="col-6">
        <form asp-controller="Genre" asp-action="Update" asp-route-id="@Model.Id" method="post">
            <div class="mb-3">
                <label for="Name" class="form-label">Название жанра: </label>
                <input type="text" class="form-control" value="@Model.Name" name="Name" placeholder="Название жанра" autocomplete="off" />
            </div>
            <button type="submit" class="btn btn-outline-info">Изменить</button></a>
        </form>